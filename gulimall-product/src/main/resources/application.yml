spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://42.192.81.113:3306/guli_pms
    driver-class-name: com.mysql.jdbc.Driver


# MapperScan
# sql映射文件位置
mybatis-plus:
  mapper-locations: classpath:/mapper/**/*.xml
  global-config:
    db-config:
      id-type: auto

server:
  port: 10000